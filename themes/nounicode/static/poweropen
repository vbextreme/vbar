name = poweropen
text.full = "power"
text.short = "power"
icon = "⏻"

event = "${module.hide(static,poweropen)}${module.show(static,logout)}${module.show(static,reboot)}${module.show(static,power)}${module.show(static,powerclose)}${modules.refresh()}"

